declare boolean : top
declare true : boolean
declare false : boolean 

def not(x : boolean) : boolean =>
  match x with
  | true => false
  | ?other => true

def not-not-not-true : boolean =>
  not(not(not(true)))
